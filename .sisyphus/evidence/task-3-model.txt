Task 3: QueryLog SQLAlchemy Model Verification
===============================================

COMPLETED: 2025-03-04

File Created:
- backend/app/models/query_log.py

QueryLog Model Schema:
- __tablename__ = "query_logs"
- id (str, primary_key, UUID string)
- tenant_id (str, indexed)
- question (str, max 2000)
- answer (str, max 10000)
- nb_sources (int, default 0)
- duration_ms (float, default 0.0)
- success (bool, default True)
- created_at (DateTime with UTC timezone)
- Composite Index: ix_query_log_tenant_created (tenant_id, created_at)

Import Verification Command:
uv run python -c "from app.models.query_log import QueryLog; cols = [c.name for c in QueryLog.__table__.columns]; assert 'tenant_id' in cols; assert 'duration_ms' in cols; print('MODEL_OK', QueryLog.__tablename__)"

Result: MODEL_OK query_logs ✓

Updated Files:
- backend/app/models/__init__.py: Added QueryLog import

Notes:
- Follows SQLAlchemy async pattern from existing models (user.py, document.py)
- Uses Mapped[] and mapped_column for type hints
- DateTime uses timezone=True and UTC default per project standard
- Includes __repr__ for debugging
- Model automatically registered via models/__init__.py import
